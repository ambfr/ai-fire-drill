AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: AI Fire Drill — commit regression analyzer backend (FastAPI + Mangum)

Parameters:
  GroqApiKey:
    Type: String
    NoEcho: true
    Description: Groq API key, passed at deploy time, not stored in this file
  GithubToken:
    Type: String
    NoEcho: true
    Default: ""
    Description: Optional GitHub token for higher API rate limits and private repos

Globals:
  Function:
    Runtime: python3.12
    Timeout: 120
    MemorySize: 512

Resources:
  AnalysesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: ai-fire-drill-analyses
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: incident_id
          AttributeType: S
      KeySchema:
        - AttributeName: incident_id
          KeyType: HASH

  ApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: backend/
      Handler: app.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref AnalysesTable
          GROQ_API_KEY: !Ref GroqApiKey
          GITHUB_TOKEN: !Ref GithubToken
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref AnalysesTable }
      Events:
        RootApi:
          Type: Api
          Properties:
            Path: /
            Method: ANY
        ProxyApi:
          Type: Api
          Properties:
            Path: /{proxy+}
            Method: ANY

Outputs:
  ApiUrl:
    Description: API Gateway endpoint
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"